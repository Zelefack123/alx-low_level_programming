#include <stdio.h>
/**
 * main - thid
 * Return: thiss
 */
void print_alphabet_x10(void);
int main(void)
{
        void print_alphabet_x10(void);
        return(0);
}
void print_alphabet_x10(void)
{
        for(i = 1 ; i <= 10; i++)
        {
        for (i = 'a' ; i <= 'z' ; i++)
        {
        putchar(i);
        }
        putchar('\n');
        }

