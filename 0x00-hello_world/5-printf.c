#include <stdio.h>
/**
 *main - its the first thing to run doing compilation
 *Return: it returns 0
 */
int main(void)
{printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
